using APAdmin.Application.ClassAttendanceModule.Commands.Register;
using Microsoft.Extensions.Logging;

namespace APAdmin.UnitTests;

[TestClass]
public class RegisterAttendanceHandlerUnitTest
{
    [TestMethod]
    public void TestMethod1()
    {
        //var list = new List<byte[]>();

        //var file = File.ReadAllBytes("mex-yvqr-hyq.csv");

        //list.Add(file);

        //var request = new RegisterAttendanceRequest(list);

        //var loggerRegister = new LoggerFactory().CreateLogger<RegisterAttendanceHandler>();

        //var handler = new RegisterAttendanceHandler(loggerRegister);

        //var response = handler.Handle(request, CancellationToken.None);


    }
}